import { MismatchPasswordsDirective } from './mismatch-passwords.directive';

describe('MismatchPasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new MismatchPasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
